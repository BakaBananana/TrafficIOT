# Mosquitto MQTT Broker Configuration
# Allow anonymous connections (dev/local only)
listener 1883
allow_anonymous true

# WebSocket listener (for browser-based MQTT clients)
listener 9001
protocol websockets
allow_anonymous true

# Logging
log_type all
log_dest stdout
